[project]
name = "algorithms-and-structures"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
testpaths = ["solutions", "structures"]
pythonpath = ["."]
python_files = ["test_*.py"]
addopts = "--import-mode=importlib"
